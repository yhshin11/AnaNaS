# Source files to compile 
FILES := EventServer
FILES += Sample 
FILES += Vertex Candidate CandInfo
FILES += EventManager
FILES += SampleAnalysis
FILES += PseudoExp

# Header files to use for dictionary generation
DICTFILES := $(FILES) LinkDef

